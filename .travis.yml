language: ruby
sudo: false
before_install:
  - rm Gemfile.lock || true
bundler_args: --without development
script:
  - "bundle exec rake spec"
notifications:
  email: true
rvm:
  - 2.3.0-dev
  - 1.9.3

env:
  - PUPPET_VERSION="~> 3.5.0"
  - PUPPET_VERSION="~> 4.0.0"
  - PUPPET_VERSION="~> 4.2.0"
  - PUPPET_VERSION=">= 0" # should be latest?

matrix:
  exclude:
    - rvm: 2.3.0-dev
      env: PUPPET_VERSION="~> 3.5.0"
